PR: #PR_NUMBER_PENDING
PR URL: PR_URL_PENDING
Branch: feat/unified-inbox-contractor-v1

# Implementation Summary

## Mission
Implement the contractor unified inbox GET route using the role-safe unified inbox data layer.

## Files Changed
- `rentchain-api/src/routes/contractorPortalRoutes.ts`
- `rentchain-api/src/routes/contractor.test.ts`
- `rentchain-api/src/services/unifiedInbox/types.ts`
- `rentchain-api/src/services/unifiedInbox/contractorInboxAdapters.ts`
- `rentchain-api/src/services/unifiedInbox/deriveUnifiedInbox.ts`
- `rentchain-api/src/services/unifiedInbox/index.ts`
- `rentchain-api/src/tests/unifiedInbox/contractorInboxAdapters.test.ts`

## What Changed
- Added `GET /api/contractor/inbox` under the existing contractor route mount.
- Added contractor-only identity enforcement so tenant, landlord, and admin roles are rejected.
- Added query validation for `limit`, `offset`, `source`, `dateFrom`, and `dateTo`.
- Loaded existing contractor source collections read-only and derived safe contractor inbox events through the unified inbox data layer.
- Added contractor inbox source kinds and adapters for work orders and contractor messages.
- Returned safe projected items with `total`, `limit`, and `offset` metadata.
- Added route-level tests for auth, role rejection, missing identity, scope rejection, query validation, filtering, pagination, empty results, and sensitive-field exclusion.
- Added data-layer tests for contractor projection safety, scoped filtering, safe references, and sensitive source rejection.

## Governance
- Scope limited to backend contractor inbox route integration, unified inbox data-layer support, and tests.
- No UI, tenant route, landlord route, persistence writes, realtime delivery, Firestore rules, billing, screening adapter, pricing, deployment, or dependency changes.
- Contractor identity is resolved server-side; client-provided contractor scope is rejected if it does not match the authenticated user.
- Response items are generated by the safe unified inbox data layer and use safe identifiers.
- Unsafe source records with admin metadata, provider payloads, storage paths, tokens, secrets, or raw IDs are excluded.

## Validation
- `npm --prefix rentchain-api test -- src/routes/contractor.test.ts`: PASS, 1 file, 10 tests.
- `npm --prefix rentchain-api test -- src/tests/unifiedInbox/`: PASS, 5 files, 16 tests.
- `npm --prefix rentchain-api run build`: PASS.
- `git diff --check`: PASS.

## Manual QA
- Not completed locally. This mission adds backend API behavior and route-level automated coverage.
- Manual QA is required before merge because a new authenticated backend route was added.

## Known Limitations
- The route derives a bounded safe item set using the current data-layer pagination limit before applying offset response metadata.
- Read-state persistence, realtime delivery, contractor UI integration, and admin/support inspection remain deferred.
- Local route tests use mocked auth and Firestore-style data; preview validation requires deployed backend freshness.

## Recommended Follow-Up
- Run manual QA against the deployed backend for 401 no auth, 403 wrong role, invalid query 400 responses, and safe 200 response shape.
- Implement unified inbox UI integration only after landlord, tenant, and contractor route surfaces have passed manual QA.
