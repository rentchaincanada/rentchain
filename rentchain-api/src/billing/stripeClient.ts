// Skeleton Stripe client helper (pure stub; no Stripe SDK dependency).
export function getStripeClient(): any | null {
  console.warn(
    "[billing] Stripe client not configured – returning null stub"
  );
  return null;
}
